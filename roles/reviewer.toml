# Role used when spawning review-style subagents
description = "Code reviewer: read-focused, high standards on design and regressions"
default_capability_mode = "read-only"
reasoning_effort = "high"
default_fork_context = true
